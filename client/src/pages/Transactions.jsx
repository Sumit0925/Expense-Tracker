import React from 'react';
import TransactionList from '../components/TransactionList';

// Shows all transaction history (income and expenses)
const Transactions = () => (
  <div>
    <TransactionList />
  </div>
);

export default Transactions;
